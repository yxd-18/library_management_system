from __future__ import annotations

import shutil
from pathlib import Path

from docx import Document


SOURCE = Path(r"D:\WeChat_files\WeChat Files\wxid_q8ndugwzao5a22\FileStorage\File\2026-05\图书管理系统研发文档(1).docx")
OUTPUT = Path(r"D:\WeChat_files\WeChat Files\wxid_q8ndugwzao5a22\FileStorage\File\2026-05\图书管理系统研发文档(1)_完善版.docx")
FALLBACK_OUTPUT = Path(r"D:\WeChat_files\WeChat Files\wxid_q8ndugwzao5a22\FileStorage\File\2026-05\图书管理系统研发文档(1)_完善版_修订.docx")


REPLACEMENTS = {
    603: "3.3.1 用户管理模块实现简介",
    604: "1. 实现架构\n用户管理模块采用“菜单交互层 + 服务封装层 + 数据访问层”的实现方式。UserMenu 负责菜单显示、输入采集与结果输出；UserMenuService 负责对登录、注册、查询、修改、删除等操作进行结果封装；UserDao 负责直接访问 PostgreSQL 中的用户数据。各层职责清晰，便于后续维护和功能扩展。",
    605: "2. 核心实现过程",
    606: "用户查询与登录相关处理：系统通过 MenuCommon 中的 promptLine、parseInt 等工具函数获取输入，再由 UserMenu 调用 UserMenuService 和 UserDao 完成用户名查询、身份校验及结果返回。UserDao 在数据库访问中使用参数化 SQL，并以 std::optional<User> 表达“查询到/未查询到”的结果，提升了安全性与可读性。",
    607: "用户维护流程：在新增读者或修改用户信息时，界面层先收集用户名、姓名、学号、电话、邮箱、状态等字段，再交由服务层调用 UserDao 执行写入或更新。删除操作增加了“不能删除当前登录管理员”的前置校验，使模块在保证功能完整的同时兼顾了基本安全性。",
    609: "3.3.2 用户管理模块相关类实现",
    610: "UserDao 类说明",
    611: "1. 核心属性：conn（PGconn*）用于保存当前 PostgreSQL 连接句柄，是用户登录、注册、查询、修改与删除等数据库操作的基础依赖。",
    612: "该属性由构造函数注入，使 UserDao 可以在统一连接上下文中执行参数化查询、解析结果集，并向上层返回用户实体对象。",
    613: "2. 主要方法说明",
    615: "UserMenu 与 UserMenuService 类说明",
    616: "1. 职责划分\nUserMenu 作为用户管理模块的控制台入口，负责打印菜单、接收输入和展示结果；UserMenuService 负责对 UserDao 返回的数据进行封装，统一生成 success、message 与 data 等返回信息，减少界面层与数据层的直接耦合。",
    617: "2. 关键实现要点",
    618: "菜单循环与输入处理：模块通过 while 循环维持菜单常驻，并结合 promptLine、parseInt、promptLongLong 等工具函数完成字符串读取与数值转换，提升了非法输入场景下的稳定性。",
    619: "业务分发与结果呈现：",
    620: "1. 查询与展示：管理员选择查询全部用户或按用户名查询时，界面层调用 listUsers、findUserByUsername 等服务函数，再将返回的用户信息按字段格式化输出。",
    621: "2. 安全校验：删除用户前增加 currentOperatorId 比较，避免误删当前登录管理员账号，降低系统被锁死的风险。",
    622: "3. 数据维护：新增与修改流程均以逐项录入方式采集用户信息，再由服务层调用 UserDao 完成持久化处理，保证界面交互与数据库写入逻辑分离。",
    662: "3.4.1 图书借阅模块实现简介",
    663: "1. 实现架构",
    664: "图书借阅模块采用“界面交互层 + 服务封装层 + 数据访问层”的实现结构。BorrowMenu 负责借书、还书、续借、预约等菜单交互；BorrowMenuService 负责统一封装业务处理结果；BorrowDao 作为核心数据访问对象，联合 BookDao、UserDao 与 OperationLogDao 完成库存校验、用户校验、记录写入和日志追踪。",
    665: "视图交互层：",
    666: "核心文件：BorrowMenu.cpp",
    667: "职责：负责展示借阅管理菜单，采集用户编号、图书编号、借阅记录编号及备注等输入信息，不直接处理底层数据库逻辑。",
    668: "服务封装层：",
    669: "核心文件：BorrowMenuService.cpp",
    670: "职责：围绕借书、还书、续借、预约、取消预约、挂失和各类记录查询等功能，统一返回 ActionResult 或 DataResult，保持界面层调用方式一致。",
    671: "数据访问层：",
    672: "核心文件：BorrowDao.cpp、BookDao.cpp、UserDao.cpp、OperationLogDao.cpp",
    673: "职责：BorrowDao 处理借阅记录与预约记录；BookDao 负责库存和图书状态相关操作；UserDao 负责用户资格校验；OperationLogDao 负责关键业务日志记录。",
    674: "2. 主要实现流程",
    675: "借书流程：界面层采集用户编号、图书编号和备注信息后，服务层调用 BorrowDao::borrowBook 执行校验与写入。",
    676: "业务校验：系统会检查用户是否存在且状态正常、是否达到借阅上限、是否存在逾期记录，以及图书当前可借库存是否充足。",
    677: "数据更新：借书成功后写入借阅记录，同时扣减图书可借库存，并补充相应的操作日志。",
    678: "还书流程：",
    679: "还书时系统根据借阅记录编号查询当前记录，确认其处于有效未归还状态。",
    680: "状态更新：系统更新归还时间、借阅状态和备注信息，并同步恢复图书库存。",
    681: "续借与预约流程：",
    682: "模块支持续借、预约、取消预约和挂失等扩展业务，并通过统一的数据访问逻辑维护借阅状态与预约状态的一致性。",
    683: "结果反馈：",
    684: "各项操作完成后，BorrowMenuService 会返回明确的提示信息，界面层据此输出成功或失败结果，使业务处理过程具备较好的可追踪性。",
    685: "模块特点：",
    686: "借阅规则、逾期刷新和预约状态流转被集中在借阅模块内部处理，减少了菜单层中分散判断的情况。",
    687: "同时，模块支持全部借阅记录、未归还记录、逾期记录、按用户查询及预约记录查询，满足日常管理与排查需要。",
    688: "异常处理：",
    689: "当输入记录不存在、库存不足、账号异常或业务条件不满足时，模块会返回明确失败信息，而不会直接进入下一步数据库写入。",
    690: "整体来看：",
    691: "该模块实现了借阅流通、预约管理与状态刷新等核心业务，是系统中业务规则最集中的功能模块之一。",
    692: "",
    693: "",
    694: "",
    696: "3.4.2 图书借阅模块相关类实现",
    697: "BorrowDao 类说明",
    698: "对应文件：BorrowDao.cpp",
    699: "职责：负责借阅记录、预约记录以及逾期状态刷新的核心数据访问逻辑，是图书借阅模块的主要持久化入口。",
    701: "关键实现要点：",
    702: "借书处理：对外提供 borrowBook 等方法，在数据库层完成借阅记录写入，并驱动库存与日志的联动更新。",
    703: "还书与续借处理：通过 returnBook、renewBook 等方法修改记录状态、归还时间和续借次数，保证借阅生命周期的完整性。",
    704: "记录查询：支持全部记录、有效记录、逾期记录、按用户查询以及预约记录查询，便于管理员追踪业务状态。",
    706: "BorrowMenuService 类说明",
    707: "对应文件：BorrowMenuService.cpp",
    708: "职责：负责将 BorrowDao 的执行结果封装为 ActionResult 或 DataResult，向界面层返回统一的成功标志、数据和提示信息。",
    710: "图3.14 部分业务规则代码\n关键实现要点：",
    711: "结果封装：服务层不直接负责底层 SQL，而是围绕借书、还书、续借、预约、取消预约、挂失等操作组织调用，并返回统一提示。",
    712: "流程协调：对记录编号、用户编号、备注等输入进行业务层转发，保证界面层代码保持简洁。",
    713: "接口组织：模块同时提供列表查询和单条查询方法，便于控制台界面按菜单项分发功能。",
    715: "BorrowMenu 类说明",
    716: "对应文件：BorrowMenu.cpp",
    717: "职责：负责借阅模块控制台界面的菜单展示、输入采集和处理结果输出，对应图 3.15 所示界面。",
    719: "图3.15 控制台界面展示\n关键实现要点：",
    720: "菜单控制：通过 while 循环维持借阅子菜单常驻，支持借书、还书、续借、预约、取消预约、挂失和多种查询操作。",
    721: "输入处理：使用 promptLongLong、promptLine 等通用函数采集业务参数，并对非法输入保持容错。",
    722: "结果输出：根据服务层返回的 message 与数据列表打印借阅记录或预约记录，形成完整的人机交互闭环。",
    724: "BookDao 类说明",
    725: "对应文件：BookDao.cpp",
    726: "职责：在借阅模块中承担图书库存与图书状态相关的数据读取和更新，为借还书流程提供图书侧支撑。",
    727: "库存维护：借书成功后减少 availableStock，还书成功后恢复库存，并据库存变化更新图书状态。",
    728: "图书校验：提供按图书编号查询图书信息的能力，供借阅流程判断图书是否存在、是否可借。",
    730: "UserDao 类说明",
    731: "对应文件：UserDao.cpp",
    732: "职责：在借阅模块中承担用户资格校验相关的数据查询，用于确认借阅人身份和账号状态。",
    733: "用户校验：根据用户编号查询用户信息，确认记录存在。",
    734: "状态校验：结合用户状态字段判断账号是否允许继续借阅、预约或续借。",
    735: "OperationLogDao 类说明",
    736: "对应文件：OperationLogDao.cpp",
    737: "职责：负责记录借阅、归还、预约、取消预约和挂失等关键操作日志，增强系统可追溯性。",
    738: "图3.16 OperationLogDao 函数部分实现",
    739: "关键实现要点：",
    740: "日志写入通过统一接口记录操作人、操作类型、目标数据和时间信息，为后续审计与问题定位提供依据。",
    645: "用户管理模块程序流程如图 3.10 所示。模块进入 runUserMenu() 后，首先输出当前管理员编号和功能菜单，再通过 parseInt(promptLine(\"\")) 读取用户输入的菜单项。",
    646: "分支 1（查询所有用户）：系统调用 listUsers(userDao)，读取全部用户信息并在控制台逐条输出用户编号、用户名、姓名、角色和状态。",
    647: "分支 2（按用户名查询）：系统先读取用户名，再调用 findUserByUsername(userDao, username) 查询单个用户，并输出该用户的详细信息。",
    648: "分支 3（新增读者）与分支 4（修改用户）：界面层分别采集新增或修改所需字段，随后调用 registerReaderUser 或 updateExistingUser 完成持久化处理。",
    649: "分支 5（删除用户）：系统先读取待删除用户编号，并检查其是否等于当前登录管理员编号；若是，则直接拒绝删除，否则调用 removeUser 执行删除操作。",
    650: "分支 0（返回上一级）：用户选择退出当前模块后，函数直接 return，流程返回上一级菜单。整个模块采用“菜单循环 + 条件分支 + 结果反馈”的运行方式。",
    742: "视图与入口层",
    743: "BorrowMenu.cpp",
    744: "角色：负责借阅模块菜单展示、输入采集和结果输出，是借书、还书、续借、预约、挂失及查询操作的直接入口。",
    745: "跳转关系：",
    746: "根据菜单项采集用户编号、图书编号、借阅记录编号或预约记录编号等参数。",
    747: "调用 BorrowMenuService 中的 createBorrowRecord、returnBorrowedBook、renewBorrowedBook、createReservationRecord、cancelReservationRecord、reportLostBorrowedBook 等函数。",
    748: "接收 ActionResult 或 DataResult 后，将 message 和记录列表打印到控制台。",
    749: "服务封装层",
    750: "BorrowMenuService.cpp",
    751: "角色：负责统一封装借阅模块各项操作的返回结果，组织 BorrowDao 调用过程，减少界面层直接处理业务细节。",
    752: "跳转关系：",
    753: "借阅、归还、续借、预约、取消预约、挂失等写操作均通过服务层函数转发至 BorrowDao。",
    754: "全部记录、未归还记录、逾期记录、按用户查询和预约查询等读取操作也通过服务层统一返回结果结构。",
    755: "数据访问层",
    756: "BorrowDao.cpp：负责借阅记录、预约记录与逾期状态刷新的核心数据库操作，是借阅模块的主要业务执行者。",
    757: "BookDao.cpp：负责图书库存和图书状态相关数据读写，为借书、还书等流程提供图书侧支撑。",
    758: "UserDao.cpp：负责用户信息与账号状态查询，用于借阅资格校验。",
    759: "OperationLogDao.cpp：负责记录借阅、归还、预约、取消预约和挂失等关键操作日志。",
    760: "模块关系说明：BorrowMenu 不直接访问数据库，而是先调用 BorrowMenuService；服务层再调用 BorrowDao，并由 BorrowDao 在内部联动图书、用户与日志等相关数据处理逻辑。",
    769: "如图 3.18 所示，图书借阅模块程序启动后进入 runBorrowMenu() 循环，系统先显示借阅管理菜单，再根据输入菜单项分发到借书、还书、续借、预约、取消预约、挂失、记录查询和逾期刷新等具体处理分支。",
    920: "核心文件关系说明",
    921: "SystemMenu.cpp（界面交互入口）",
    922: "职责：负责展示系统维护菜单，采集管理员输入，并将“查看操作日志”“新增备份记录”“查看备份记录”“初始化默认设置”“查看/修改用户设置”等请求分发给服务层。",
    923: "SystemMenuService.cpp（服务封装层）",
    924: "职责：负责统一封装维护模块的处理结果，调用 BackupDao、OperationLogDao 和 UserSettingsDao 完成日志、备份记录和用户设置相关操作。",
    925: "BackupDao.cpp（备份记录数据访问层）",
    926: "职责：负责向 backup_records 表写入备份记录，并查询全部备份记录。该类管理的是备份元数据，不直接执行数据库文件复制或恢复操作。",
    930: "说明：图 3.20 展示了系统维护模块的主要调用链。管理员首先通过 SystemMenu 发起请求，界面层再调用 SystemMenuService 对应函数，最后由不同 Dao 类完成实际数据访问。",
    931: "操作日志流程：SystemMenu 接收“查看操作日志”请求后，调用 listOperationLogs(logDao)，再由 OperationLogDao::getAllLogs() 读取日志列表并返回界面层展示。",
    932: "备份记录流程：管理员输入备份名称、文件路径、备份类型和备注后，SystemMenu 调用 createBackupLog(...)，再由 BackupDao::addBackupRecord() 将记录写入数据库并同步留下操作日志。",
    933: "备份记录查询流程：管理员选择“查看全部备份记录”后，SystemMenu 调用 listBackupLogs(backupDao)，再由 BackupDao::getAllBackupRecords() 返回完整备份记录列表。",
    934: "用户设置初始化流程：管理员选择初始化默认设置时，服务层调用 UserSettingsDao::ensureDefaultSettings(userId) 为当前用户生成默认设置项。",
    935: "用户设置查询流程：系统支持查询当前用户设置和全部用户设置，分别通过 getUserSettings 与 listUserSettings 调用 UserSettingsDao 对应接口完成。",
    936: "用户设置修改流程：管理员输入字号、主题、语言、分页条数和通知开关后，SystemMenu 调用 updateUserSettingsProfile(...)，再由 UserSettingsDao::updateSettings(...) 完成写入。",
    937: "依赖关系上，SystemMenu 只负责交互，SystemMenuService 只负责封装结果和组织调用，底层数据读写分别由 OperationLogDao、BackupDao 和 UserSettingsDao 承担，结构较为清晰。",
    938: "",
    939: "",
    942: "如图 3.21 所示，系统维护模块程序流程围绕“菜单输入 -> 功能分发 -> 结果反馈 -> 返回菜单”展开。",
    945: "说明：1. 初始化与输入阶段",
    946: "启动：程序执行 runSystemMenu()，进入系统维护模块的主循环。",
    947: "展示：调用 printSystemMenu() 输出维护菜单，包括查看操作日志、新增备份记录、查看备份记录、初始化默认设置、查看当前用户设置、查看全部用户设置、修改当前用户设置和返回上一级等选项。",
    948: "输入：通过 parseInt(promptLine(\"\")) 读取菜单项，若输入无效则提示重新输入。",
    949: "该流程复用了 MenuCommon 中的通用输入处理函数，保证了非法输入时模块仍能保持可控运行。",
    950: "2. 主要分支处理",
    951: "系统根据管理员输入的菜单编号，将流程分发为以下几类主要路径：",
    952: "路径 A：查看操作日志（键值 = 1）",
    953: "动作：调用 listOperationLogs(logDao)，再由 OperationLogDao::getAllLogs() 返回日志集合。",
    954: "反馈：系统逐条输出日志编号、操作人、操作类型、目标表、目标记录和操作时间等信息。",
    955: "路径 B：新增备份记录（键值 = 2）",
    956: "动作：依次读取备份名称、文件路径、备份类型和备注，再调用 createBackupLog(...) 与 BackupDao::addBackupRecord(...) 写入记录。",
    957: "反馈：系统提示“新增备份记录成功”或“新增备份记录失败”。",
    958: "路径 C：查看全部备份记录（键值 = 3）",
    959: "动作：调用 listBackupLogs(backupDao)，再由 BackupDao::getAllBackupRecords() 返回记录列表。",
    960: "反馈：系统输出备份记录编号、名称、路径、类型、操作人和时间等信息。",
    961: "路径 D：用户设置管理（键值 = 4、5、6、7）",
    962: "动作：根据具体菜单项分别执行默认设置初始化、当前用户设置查询、全部用户设置查询或当前用户设置修改。",
    963: "反馈：各项设置类操作均通过统一返回结果提示管理员处理是否成功，并在查询时输出设置详情。",
    964: "路径 E：返回上一级（键值 = 0）",
    965: "动作：函数直接 return，结束当前维护模块循环。",
    966: "整体流程说明：",
    967: "系统维护模块没有直接处理图书借阅等核心业务，而是通过日志、备份记录和用户设置三类功能为整套系统提供运行支撑与管理能力。",
    968: "",
    969: "",
    970: "",
    971: "",
    972: "",
    973: "",
    777: "3.5.1 图书管理模块实现简介",
    778: "1. 实现架构",
    779: "图书管理模块采用“界面交互层 + 服务封装层 + 数据访问层 + 分类辅助层”的实现结构，既支持图书基础信息维护，也支持分类数据的同步管理。",
    780: "界面交互层：BookMenu 负责打印菜单、采集输入并输出图书或分类查询结果，是管理员进行图书维护的直接入口。",
    781: "服务封装层：BookMenuService 负责组织新增、查询、修改、删除等业务操作，统一向界面层返回处理结果。",
    782: "数据访问层：BookDao 负责图书表的增删改查，CategoryDao 负责分类表的增删改查，共同支撑馆藏数据维护。",
    783: "基础支撑层：数据库连接、日志记录及输入解析由 DBUtil、OperationLogDao、MenuCommon 等通用组件提供支持。",
    784: "2. 主要实现流程",
    785: "模块启动后先显示图书管理菜单，管理员可根据需要执行新增图书、查询图书、关键词检索、修改图书、删除图书以及分类管理等操作。",
    786: "菜单与输入：BookMenu 通过 promptInt、promptLongLong 和 promptLine 采集图书编号、分类编号及图书各项属性，并完成基础格式转换。",
    787: "查询流程：系统支持查询全部图书、按图书 ID 查询以及关键词检索，查询结果统一通过 printBook 等函数进行格式化展示。",
    788: "新增流程：新增图书时，服务层会校验必要字段和库存数据，再调用 BookDao 写入数据库，同时记录操作日志。",
    789: "修改流程：修改图书时，系统根据输入的图书信息调用 updateExistingBook 和 BookDao::updateBook 完成数据更新。",
    790: "删除流程：删除图书时，系统根据图书编号执行删除逻辑，并通过返回结果提示管理员操作是否成功。",
    791: "分类管理流程：模块同时提供图书分类的新增、查询、修改和删除功能，使图书基础数据与分类数据能够统一维护。",
    792: "循环控制：每次操作完成后系统都会返回图书管理菜单，等待下一次指令，直到管理员主动退出当前模块。",
    793: "结果反馈：各项业务执行后均通过统一 message 输出成功或失败原因，提升了管理员操作的可理解性。",
    794: "整体来看，该模块实现了图书信息与分类信息的集中维护，是支撑馆藏数据管理的基础模块。",
    796: "3.5.2 图书管理模块相关类实现",
    797: "图书管理模块主要由 Book 实体类、BookDao 数据访问类、CategoryDao 分类访问类以及 BookMenu 界面控制类组成，各类职责划分较为明确。",
    798: "1. 实体类：Book",
    799: "Book 定义在 Book.h 中，采用结构体形式封装图书基础信息、库存信息、分类信息和状态信息，是模块间传递图书数据的统一载体。",
    802: "核心属性说明：",
    803: "基础信息字段包括 bookId、isbn、bookTitle、author、publisher、publishDate、price 和 description，用于描述图书的基本资料。",
    804: "分类信息通过 categoryId 和 categoryName 关联图书类别，便于分类展示与条件查询。",
    805: "库存与状态字段：",
    806: "totalStock 和 availableStock 分别表示总库存与当前可借数量，是借阅与归还流程进行库存判断的直接依据。",
    807: "lostCount、pendingReservationCount、readyReservationCount 等字段用于描述遗失和预约相关状态，便于后续业务扩展。",
    808: "bookStatus 用于表示当前图书状态，如可借、库存不足或下架等，便于界面展示与业务判断。",
    809: "location、createTime、updateTime 等字段分别记录馆藏位置和数据变更时间，提高数据的可维护性。",
    810: "2. 数据访问类：BookDao",
    811: "BookDao 负责图书数据的持久化处理，是图书业务与 PostgreSQL 数据库之间的直接桥梁，同时会结合 OperationLogDao 记录关键写操作。",
    814: "核心职责：",
    815: "数据维护：对外提供 addBook、getAllBooks、getBookById、searchBooks、updateBook 和 deleteBook 等方法，覆盖图书全生命周期操作。",
    816: "状态计算：通过 calculateBookStatus 根据库存变化自动推导图书状态，减少界面层和服务层的重复判断。",
    817: "日志追踪：在新增、修改、删除等写操作中带入 operatorId，便于记录操作来源并增强可追溯性。",
    818: "关键接口：",
    819: "getBookById 使用 std::optional<Book> 返回查询结果，能够清晰表示“查询到图书”和“图书不存在”两种状态。",
    820: "searchBooks 支持按关键词检索图书，方便管理员快速完成模糊查询。",
    821: "3. 界面控制类：BookMenu",
    822: "BookMenu 以 runBookMenu 为入口，负责图书管理菜单的展示、参数采集以及图书与分类结果的输出，是管理员进行图书维护的直接交互界面。",
    823: "图3.15 BookMenu 类入口点打印函数",
    824: "核心职责：",
    825: "流程控制：根据菜单编号分发到新增、查询、修改、删除图书以及分类管理等不同处理分支。",
    826: "依赖注入：通过参数接收 BookDao、CategoryDao 和当前操作人编号，保持界面层与数据访问层的低耦合。",
    827: "交互协调：将输入采集、服务调用和结果输出组织为完整操作链路，使管理员可以连续完成图书与分类维护。",
    850: "3.6.1 系统维护模块实现简介",
    851: "1. 实现架构",
    852: "系统维护模块采用“菜单交互层 + 服务封装层 + 数据访问层”的实现方式，主要围绕操作日志、备份记录和用户设置三类管理功能展开。",
    853: "界面交互层",
    854: "核心文件：SystemMenu.cpp",
    855: "职责：负责展示系统维护菜单，包括查看操作日志、新增备份记录、查看备份记录、初始化默认设置、查看用户设置和修改用户设置等功能入口。",
    856: "模块通过 MenuCommon 中的输入工具读取管理员输入，并将对应选项分发给服务层处理。",
    857: "界面层同时负责将查询结果、处理结果与提示信息输出到控制台，形成完整的交互闭环。",
    858: "业务封装层",
    859: "核心文件：SystemMenuService.cpp",
    860: "职责：负责对日志查询、备份记录新增、用户设置查询与修改等功能进行统一封装，并向界面层返回 ActionResult 或 DataResult。",
    861: "服务层不直接处理菜单展示，而是专注于调用各个 Dao 对象并整理返回信息。",
    862: "通过统一的结果结构，系统能够在不同维护功能之间保持一致的提示风格和错误处理方式。",
    863: "数据访问层",
    864: "核心文件：OperationLogDao.cpp、BackupDao.cpp、UserSettingsDao.cpp",
    865: "职责：OperationLogDao 负责操作日志存取，BackupDao 负责备份记录管理，UserSettingsDao 负责默认设置初始化、设置查询和设置更新。",
    866: "系统维护模块实现过程",
    867: "当管理员进入系统维护模块后，SystemMenu 首先展示维护菜单并读取功能编号。",
    868: "若选择日志相关功能，系统调用 OperationLogDao 查询日志列表并输出操作人、类型、目标表和时间等信息。",
    869: "若选择备份记录相关功能，系统读取备份名称、路径、类型和备注，再通过 BackupDao 新增或查询备份记录。",
    870: "若选择用户设置相关功能，系统可初始化默认设置、查看当前用户设置、查看全部用户设置或修改当前用户设置。",
    871: "各项维护功能执行后，服务层都会返回明确的处理结果，界面层据此提示管理员当前操作是否成功。",
    872: "该模块虽然不直接处理借阅业务，但承担了日志追踪、备份记录管理和个性化设置维护等支撑性工作。",
    902: "3.6.2 系统维护模块相关类实现",
    903: "SystemMenuService 类说明",
    904: "职责：业务门面与结果封装层。",
    905: "分析：该类负责连接 SystemMenu 与各个维护类接口，统一封装日志查询、备份记录新增、用户设置初始化、查询与修改等操作结果，减少界面层直接依赖多个 Dao 的复杂度。",
    906: "2. BackupDao 类说明",
    907: "职责：备份记录的数据访问与审计。",
    908: "分析：BackupDao 负责向 backup_records 表写入备份记录，并提供全部备份记录的查询能力。其实现并不直接执行数据库文件复制，而是对备份元数据进行登记和读取。",
    909: "关键点：新增记录时会校验备份名称、文件路径和备份类型等必要字段，并写入操作日志。",
    910: "查询能力：支持按时间倒序读取备份记录，返回备份名称、路径、类型、操作人和备注等信息。",
    911: "3. OperationLogDao 类说明",
    912: "职责：操作日志的数据存取。",
    913: "分析：OperationLogDao 负责记录和查询系统中的关键操作行为，是用户管理、图书管理、借阅管理和系统维护等模块共享的审计组件。",
    914: "读取：通过 getAllLogs 等接口返回日志集合，便于管理员查看历史操作记录。",
    915: "写入：通过统一 addLog 接口记录操作人、操作类型、目标表、目标记录和操作内容等信息。",
    916: "4. UserSettingsDao 类说明",
    917: "职责：用户个性化设置的初始化、查询与修改。该类负责维护字体大小、主题、语言、分页条数和通知开关等设置数据，为系统维护模块补充了用户设置管理能力。",
}


def main() -> None:
    if not SOURCE.exists():
        raise FileNotFoundError(f"Source file not found: {SOURCE}")

    destination = OUTPUT
    try:
        shutil.copyfile(SOURCE, destination)
    except PermissionError:
        destination = FALLBACK_OUTPUT
        shutil.copyfile(SOURCE, destination)

    doc = Document(destination)

    for index, text in REPLACEMENTS.items():
        doc.paragraphs[index].text = text

    doc.save(destination)
    print(str(destination))


if __name__ == "__main__":
    main()
